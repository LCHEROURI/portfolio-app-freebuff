# Review 2026-08-06 (main) — verify:all fold + token resolution dedup

Scope: commit 2875241 (docs/launch.md, scripts/verify-all.mjs,
scripts/verify-deployed-hash.mjs, scripts/verify-launch-checklist.mjs,
scripts/verify-token-health.mjs). The change landed before the review was
requested, so the working tree was clean and the review covers the committed
diff 2a86034..2875241. Reviewed on the deepseek flash reviewer; the author is
also on the deepseek family, so this is not a true cross model read — switch
your model with /model and re-run for a fully independent pass.

Verdict: Approve with nits.

Blockers: none.

Major: none.

Minor:

1. The EXPIRED branch (negative daysLeft) is nearly unreachable: an expired
   token fails GET /v2/user/tokens first, so the gate exits before the
   daysLeft check. Defensive only; comment it as such so a future reader does
   not treat it as the primary expiry guard.
2. InvalidTokenError is imported and re-exported only to serve the test
   import surface; the script never constructs it. Import it in the test
   from verify-deployed-hash.mjs instead, or keep the re-export knowingly.
3. The EXPIRED exit 1 path is untested; extract the daysLeft decision into a
   pure helper for a test in the existing style of the suite.
4. The EXPIRED line uses console.log (stdout) while RESULT: FAIL uses
   console.error (stderr). Use console.error for both so a caller capturing
   stderr sees the whole story.

Strengths: single source of truth for readToken (the dedup is correctly
scoped and re-exports keep the test surface stable); the launch checklist
drift guard, verify:all gate table, and docs were updated in lockstep
(7 to 8) so the three surfaces cannot silently disagree.
