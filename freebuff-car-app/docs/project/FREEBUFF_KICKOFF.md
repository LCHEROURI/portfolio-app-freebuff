# FREEBUFF_KICKOFF.md — freebuff-car-app

**Project:** freebuff-car-app
**Role:** Senior full-stack builder working under strict owner approval.

## Before editing

1. Confirm the current repository is **freebuff-car-app** (inside `portfolio-app-freebuff`).
2. Read this file and the current prompt/spec in `docs/project/`.
3. Inspect the current branch and `git status`.
4. Work only on the scope given.
5. Use a branch beginning with `freebuff/` for feature work.
6. Do not merge, deploy, create production credentials, or change external services without explicit approval.

## Non-negotiable rules

- Work only in the **freebuff-car-app** directory inside `portfolio-app-freebuff`.
- Before editing, report repository, branch, and git status.
- Never merge to main or deploy without explicit owner approval.
- Never create or expose production credentials unless explicitly authorized.
- Do not connect to dealership systems or use third-party production APIs without approval.
- Keep financial math deterministic and isolated in tested utility functions.
- Do not invent calculations inside UI components.
- Use branch prefix `freebuff/`.
- Keep each task scoped to the current prompt/phase.
- Add or update tests for changed behavior.
- Run tests and `npm run build` before claiming completion.
- Update `docs/project/IMPLEMENTATION_LOG.md`.
- If instructions conflict, scope expands, or the repo is wrong: STOP and ask.

## Completion report

When done, report:
1. Files changed.
2. Tests added/updated.
3. Exact test/build commands run and results.
4. Browser behavior to manually verify.
5. Any unresolved risks.
6. Current branch and whether a Draft PR is ready.

## Current phase

See `docs/project/freebuff-car-app-prompt-sequence.md` for the active prompt.
